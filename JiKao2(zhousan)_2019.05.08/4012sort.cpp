#include <bits/stdc++.h>

using namespace std;


long long n, x;
vector <long long> v;

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	cin >> n;
	for(int i = 1; i <= n; i++){
		cin >> x;
		v.push_back(x);
	}

	sort(v.begin(), v.end() + 1);

	for(int i = 0; i < v.size(); i++){
		k += 
	}




	return 0;
}
